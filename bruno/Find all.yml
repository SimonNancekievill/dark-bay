info:
  name: Find all
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/auctions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
