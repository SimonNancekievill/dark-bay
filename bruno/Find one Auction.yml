info:
  name: Find one Auction
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/auctions/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
