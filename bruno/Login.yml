info:
  name: Login
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseURL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "felix",
        "password": "ichBin3inHase"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
