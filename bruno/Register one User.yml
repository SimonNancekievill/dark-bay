info:
  name: Register one User
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}/users/register"
  body:
    type: json
    data: |-
      {
        "username": "pomodoro",
        "password": "iAmTheRedOne"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
