info:
  name: Update Auction
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseURL}}/auctions/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
