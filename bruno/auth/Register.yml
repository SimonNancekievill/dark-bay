info:
  name: Register
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "newuser",
        "password": "ichBin3inHase"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
