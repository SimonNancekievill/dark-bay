info:
  name: Find all Offers of Auction
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/auctions/:id/offers"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
